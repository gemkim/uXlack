const auth = {
  registerUser: '/auth/registerUser',
  login: '/auth/login',
  logout: '/auth/logout'
}
const project = {
  create: '/project/create',
  getProjectList: '/project/getProjectList'
}

export const API_ENDPOINT = { auth, project }
